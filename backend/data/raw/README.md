# Raw datasource mirror

This directory contains 1:1 copies of the selected source files used by the app.

Files are copied from `SmartCityMagdeburg2026/Datasources` without reshaping so
the backend can derive runtime summaries while keeping the original data intact.
